#include <iostream>
#include "Functions.h"
using namespace std;

int main() {
    int new_size;
    int size;
    int *arr2[new_size];
    int *arr= new int(size);
    resize(*&arr,size, new_size);
    cout<<"\nSIR KINDLY RUN THE TASKS INDIVIDUALLY "<<endl;
//     mergeArray();
    return 0;
}
